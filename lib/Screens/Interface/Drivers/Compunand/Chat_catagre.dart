import 'package:flutter/material.dart';

class chat_catage extends StatelessWidget {
  const chat_catage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
